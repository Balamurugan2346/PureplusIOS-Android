import { StyleSheet, Text, View } from 'react-native'

const ShowToast = () => {
  return (
    <View>
      <Text>ShowToast</Text>
    </View>
  )
}

export default ShowToast

const styles = StyleSheet.create({})